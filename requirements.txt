# Daftar dependensi yang sebenarnya ada di pyproject.toml — berkas ini hanya
# jalan pintas supaya `pip install -r requirements.txt` tetap bekerja, tanpa
# menyalin daftarnya dua kali (yang cepat sekali jadi tidak sinkron).
#
#   pip install -e .            pustaka saja
#   pip install -e ".[semua]"   + REST API, MCP, dan pytest
-e .[semua]
